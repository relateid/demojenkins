node {
    git 'https://github.com/relateid/demojenkins.git'
    if (!fileExists ('Dockerfile')) {
      echo 'NO Docker file
    }
    if (fileExists ('Dockerfile')) {
      echo 'YES Docker file
    }
    
}
